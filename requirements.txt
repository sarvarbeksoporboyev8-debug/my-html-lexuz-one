# Core dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
python-dotenv>=1.0.0
numpy>=1.24.0

# Embeddings (torch installed separately in Dockerfile for CPU-only version)
sentence-transformers>=2.2.0

# PDF processing
pypdf>=3.0.0
pymupdf>=1.22.0

# OCR
pytesseract>=0.3.10
pillow>=9.0.0

# DuckDuckGo search (primary path in search_ask.py)
duckduckgo-search>=7.0.0
